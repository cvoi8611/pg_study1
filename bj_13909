#include <stdio.h>

int main()
{
    int n,w,l;
    l = 21000000000;
    int A[l];
    scanf("%s",&n);
    
    for (int i=0; i<sqrt(l); i++){
        for (int j=0; i<l; j--){
            if (A[j] == 1){
                A[j] = 0;
        }
        else
        A[j] = 1;
    }
    A[]

}